<Page x:Class="ChatBotClient.Views.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d"
      Background="{StaticResource BackgroundBrush}"
      UseLayoutRounding="True"
      SnapsToDevicePixels="True">

    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Настройки" FontSize="20" Style="{StaticResource BaseTextStyle}" Margin="0,0,0,20"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Имя пользователя:" Style="{StaticResource BaseTextStyle}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource ModernTextBoxStyle}" Width="200"/>
            </StackPanel>
            <Button Content="Сохранить" Style="{StaticResource ModernButtonStyle}" Command="{Binding SaveSettingsCommand}"/>
        </StackPanel>
    </Grid>
</Page>