<Page x:Class="ChatBotClient.Features.Chat.Views.ChatPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d"
      Background="{StaticResource BackgroundBrush}"
      UseLayoutRounding="True"
      SnapsToDevicePixels="True"
      TextOptions.TextFormattingMode="Display">

    <Grid>
        <Border Style="{StaticResource BaseBorderStyle}" Margin="20" Effect="{StaticResource DropShadowEffect}">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Заголовок -->
                <TextBlock Text="Чат с ботом" Style="{StaticResource BaseTextStyle}" FontSize="18" FontWeight="SemiBold"
                           Margin="0,0,0,10"/>

                <!-- История сообщений -->
                <Border Grid.Row="1" Style="{StaticResource BaseBorderStyle}" CornerRadius="10" Padding="10"
                        Effect="{StaticResource DropShadowEffect}">
                    <ListBox ItemsSource="{Binding Messages}" Style="{StaticResource ListBoxStyle}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid MaxWidth="500" Margin="5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Background="{Binding Author, Converter={StaticResource MessageConverter}}"
                                            Style="{StaticResource MessageBubbleStyle}"
                                            HorizontalAlignment="{Binding Author, Converter={StaticResource MessageConverter}, ConverterParameter=Alignment}"
                                            Grid.Column="{Binding Author, Converter={StaticResource MessageConverter}, ConverterParameter=GridColumn}"
                                            Padding="10" CornerRadius="10">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Text}" TextWrapping="Wrap"
                                                       Style="{StaticResource BaseTextStyle}"/>
                                            <TextBlock Text="{Binding Timestamp, StringFormat='HH:mm'}"
                                                       Style="{StaticResource BaseTextStyle}" FontSize="10"
                                                       Foreground="{StaticResource DividerBrush}" HorizontalAlignment="Right"/>
                                            <!-- Кнопка для упражнения -->
                                            <Button Content="Попробовать упражнение" Style="{StaticResource ModernButtonStyle}"
                                                    Command="{Binding DataContext.TryExerciseCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                    Visibility="{Binding Author, Converter={StaticResource StringToVisibilityConverter}, ConverterParameter=Bot}"
                                                    Margin="0,5,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>

                <!-- Панель ввода -->
                <TextBox Grid.Row="2" Text="{Binding MessageText, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource ModernTextBoxStyle}" Margin="0,20,0,0"
                         AcceptsReturn="False" AcceptsTab="False"
                         materialDesign:HintAssist.Hint="Введите сообщение...">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter" Command="{Binding SendMessageCommand}"/>
                    </TextBox.InputBindings>
                    <TextBox.Template>
                        <ControlTemplate TargetType="TextBox">
                            <Grid>
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="40"/>
                                            <ColumnDefinition Width="40"/>
                                            <ColumnDefinition Width="40"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="40"/>
                                            <ColumnDefinition Width="40"/>
                                        </Grid.ColumnDefinitions>
                                        <Button Grid.Column="0" Content="😊" Style="{StaticResource IconButtonStyle}"
                                                ToolTip="Добавить эмодзи" Command="{Binding AddEmojiCommand}"/>
                                        <Button Grid.Column="1" Content="📎" Style="{StaticResource IconButtonStyle}"
                                                ToolTip="Прикрепить файл" Command="{Binding AttachFileCommand}"/>
                                        <Button Grid.Column="2" Content="{materialDesign:PackIcon Kind=Record}" Style="{StaticResource IconButtonStyle}"
                                                ToolTip="Записать аудио" Command="{Binding RecordAudioCommand}"/>
                                        <ScrollViewer Grid.Column="3" x:Name="PART_ContentHost" Margin="5,2"
                                                      VerticalAlignment="Center"/>
                                        <Button Grid.Column="4" Content="🎙" Style="{StaticResource IconButtonStyle}"
                                                ToolTip="Голосовой ввод" Command="{Binding VoiceInputCommand}"/>
                                        <Button Grid.Column="5" Content="➤" Style="{StaticResource IconButtonStyle}"
                                                ToolTip="Отправить" Command="{Binding SendMessageCommand}"/>
                                    </Grid>
                                </Border>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </TextBox.Template>
                    <TextBox.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Добавить эмодзи" Command="{Binding AddEmojiCommand}"/>
                            <MenuItem Header="Прикрепить файл" Command="{Binding AttachFileCommand}"/>
                            <MenuItem Header="Посмотреть историю" Command="{Binding ShowChatHistoryCommand}"/>
                            <MenuItem Header="Начать новый диалог" Command="{Binding NewDialogCommand}"/>
                        </ContextMenu>
                    </TextBox.ContextMenu>
                </TextBox>

                <!-- Панель навигации -->
                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="{materialDesign:PackIcon Kind=Book}"
                            Style="{StaticResource IconButtonStyle}" ToolTip="Перейти к дневнику"
                            Command="{Binding GoToDiaryCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Модальное окно для подтверждения нового диалога -->
        <Border x:Name="NewDialogModal" Background="{StaticResource ModalOverlayBrush}"
                Visibility="{Binding NewDialogModalVisibility, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Style="{StaticResource BaseBorderStyle}" CornerRadius="10" Padding="20"
                    Width="300" Height="150" VerticalAlignment="Center" HorizontalAlignment="Center"
                    Effect="{StaticResource DropShadowEffect}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Начать новый диалог? Текущая история будет очищена."
                               Style="{StaticResource BaseTextStyle}" TextWrapping="Wrap" FontSize="14"/>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Content="Отмена" Style="{StaticResource ModernButtonStyle}" Margin="0,0,10,0"
                                Command="{Binding CancelNewDialogCommand}"/>
                        <Button Content="Подтвердить" Style="{StaticResource ModernButtonStyle}"
                                Command="{Binding ConfirmNewDialogCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Border>

        <!-- Модальное окно для истории чатов -->
        <Border x:Name="ChatHistoryModal" Background="{StaticResource ModalOverlayBrush}"
                Visibility="{Binding ChatHistoryModalVisibility, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Style="{StaticResource BaseBorderStyle}" CornerRadius="10" Padding="20"
                    Width="400" Height="300" VerticalAlignment="Center" HorizontalAlignment="Center"
                    Effect="{StaticResource DropShadowEffect}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="История чатов" Style="{StaticResource BaseTextStyle}" FontSize="16" FontWeight="SemiBold"
                               Margin="0,0,0,10"/>
                    <ListBox Grid.Row="1" ItemsSource="{Binding ChatHistory}" Style="{StaticResource ListBoxStyle}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" Style="{StaticResource BaseTextStyle}"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <Button Grid.Row="2" Content="Закрыть" Style="{StaticResource ModernButtonStyle}"
                            HorizontalAlignment="Center" Margin="0,10,0,0" Command="{Binding CloseChatHistoryCommand}"/>
                </Grid>
            </Border>
        </Border>

        <!-- Модальное окно для упражнения -->
        <Border x:Name="ExerciseModal" Background="{StaticResource ModalOverlayBrush}"
                Visibility="{Binding IsExerciseModalVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Style="{StaticResource BaseBorderStyle}" CornerRadius="10" Padding="20"
                    Width="300" Height="200" VerticalAlignment="Center" HorizontalAlignment="Center"
                    Effect="{StaticResource DropShadowEffect}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Дыхательная техника 4-7-8: Вдох 4 сек, задержка 7 сек, выдох 8 сек."
                               TextWrapping="Wrap" Style="{StaticResource BaseTextStyle}" FontSize="14"/>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Content="Начать" Style="{StaticResource ModernButtonStyle}" Margin="0,0,10,0"
                                Command="{Binding StartExerciseCommand}"/>
                        <Button Content="Закрыть" Style="{StaticResource ModernButtonStyle}"
                                Command="{Binding CloseExerciseCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Border>

        <!-- Модальное окно для рейтинга -->
        <Border x:Name="RatingModal" Background="{StaticResource ModalOverlayBrush}"
                Visibility="{Binding IsRatingModalVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Style="{StaticResource BaseBorderStyle}" CornerRadius="10" Padding="20"
                    Width="300" Height="150" VerticalAlignment="Center" HorizontalAlignment="Center"
                    Effect="{StaticResource DropShadowEffect}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <StackPanel>
                        <TextBlock Text="Насколько тебе стало легче? (1–5)" Style="{StaticResource BaseTextStyle}" FontSize="14"/>
                        <ComboBox SelectedIndex="{Binding SessionRating}" Margin="0,10,0,0" Style="{StaticResource MaterialDesignComboBox}">
                            <ComboBoxItem Content="1"/>
                            <ComboBoxItem Content="2"/>
                            <ComboBoxItem Content="3"/>
                            <ComboBoxItem Content="4"/>
                            <ComboBoxItem Content="5"/>
                        </ComboBox>
                    </StackPanel>
                    <Button Grid.Row="1" Content="Отправить" Style="{StaticResource ModernButtonStyle}"
                            HorizontalAlignment="Right" Margin="0,10,0,0" Command="{Binding SubmitRatingCommand}"/>
                </Grid>
            </Border>
        </Border>

        <!-- Модальное окно для эмодзи -->
        <Border x:Name="EmojiModal" Background="{StaticResource ModalOverlayBrush}"
                Visibility="{Binding IsEmojiModalVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Style="{StaticResource BaseBorderStyle}" CornerRadius="10" Padding="20"
                    Width="200" Height="150" VerticalAlignment="Center" HorizontalAlignment="Center"
                    Effect="{StaticResource DropShadowEffect}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <WrapPanel>
                        <Button Content="😊" Style="{StaticResource IconButtonStyle}" Command="{Binding SelectEmojiCommand}" CommandParameter="😊"/>
                        <Button Content="😢" Style="{StaticResource IconButtonStyle}" Command="{Binding SelectEmojiCommand}" CommandParameter="😢"/>
                        <Button Content="😊" Style="{StaticResource IconButtonStyle}" Command="{Binding SelectEmojiCommand}" CommandParameter="😊"/>
                        <Button Content="😡" Style="{StaticResource IconButtonStyle}" Command="{Binding SelectEmojiCommand}" CommandParameter="😡"/>
                    </WrapPanel>
                    <Button Grid.Row="1" Content="Закрыть" Style="{StaticResource ModernButtonStyle}"
                            HorizontalAlignment="Right" Margin="0,10,0,0" Command="{Binding CloseEmojiModalCommand}"/>
                </Grid>
            </Border>
        </Border>
    </Grid>
</Page>