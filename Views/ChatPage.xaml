<Window x:Class="ChatBotClient.Views.ChatPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="ChatBot" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0">
            <ItemsControl ItemsSource="{Binding Messages}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Black" BorderThickness="1" Margin="5" Padding="10">
                            <StackPanel Orientation="Horizontal">
                                <StackPanel>
                                    <TextBlock Text="{Binding Author}" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Text}"/>
                                    <TextBlock Text="{Binding Timestamp, StringFormat='{}{0:HH:mm:ss}'}" FontStyle="Italic" FontSize="10"/>
                                </StackPanel>
                                <TextBlock Text="{Binding Status}" Margin="10,0,0,0" Foreground="Gray" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <DockPanel Grid.Row="1" LastChildFill="True">
            <Button Content="Send" Command="{Binding SendCommand}" DockPanel.Dock="Right" Margin="5"/>
            <TextBox x:Name="InputTextBox" Text="{Binding InputText}" Width="300" Margin="5"/>
        </DockPanel>
    </Grid>
</Window>