<Page x:Class="ChatBotClient.Views.DiaryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:converters="clr-namespace:ChatBotClient.Converters"
      mc:Ignorable="d"
      Background="{StaticResource BackgroundBrush}"
      UseLayoutRounding="True"
      SnapsToDevicePixels="True"
      TextOptions.TextFormattingMode="Display">
    <Grid>
        <Border Style="{StaticResource BaseBorderStyle}" Margin="20" CornerRadius="20"
                Effect="{StaticResource DropShadowEffect}">
            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Боковая панель тегов -->
                <Border Grid.RowSpan="4" Background="{StaticResource PaperHoverBrush}" CornerRadius="10" Padding="10"
                        Effect="{StaticResource DropShadowEffect}" VerticalAlignment="Top">
                    <StackPanel>
                        <TextBlock Text="Теги" FontSize="16" FontWeight="SemiBold" Style="{StaticResource BaseTextStyle}"
                                   Margin="0,0,0,10"/>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Text="{Binding NewTag, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ModernTextBoxWithHintStyle}" Margin="0,0,5,0"/>
                            <Button Grid.Column="1" Content="+" Style="{StaticResource ModernButtonStyle}"
                                    Padding="8" Command="{Binding AddTagCommand}"/>
                        </Grid>
                        <ItemsControl ItemsSource="{Binding Tags}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <ToggleButton Content="{Binding}" Style="{StaticResource ModernToggleButtonStyle}"
                                                  Margin="0,0,0,5" IsChecked="{Binding IsSelected}"
                                                  Command="{Binding DataContext.SelectTagCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                  CommandParameter="{Binding}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- Верхняя панель -->
                <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Top">
                    <TextBlock Text="Дневник" FontSize="18" FontWeight="SemiBold" Style="{StaticResource BaseTextStyle}"
                               Margin="0,0,0,10"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <RadioButton Content="Записи" Style="{StaticResource TabStyle}" IsChecked="{Binding IsEntriesTabSelected}"/>
                        <RadioButton Content="Настройки дневника" Style="{StaticResource TabStyle}" IsChecked="{Binding IsSettingsTabSelected}"/>
                    </StackPanel>
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource ModernTextBoxStyle}" materialDesign:HintAssist.Hint="Поиск записей..."/>
                        <ComboBox Grid.Column="1" SelectedIndex="{Binding SortMode}" Margin="10,0,10,0" Width="100">
                            <ComboBoxItem Content="По дате"/>
                            <ComboBoxItem Content="По тегу"/>
                        </ComboBox>
                        <Button Grid.Column="2" Content="Фильтры" Style="{StaticResource ModernButtonStyle}"
                                Command="{Binding ShowFiltersCommand}"/>
                    </Grid>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <Button Content="Создать заметку" Style="{StaticResource ModernButtonStyle}" Margin="0,0,10,0"
                                Command="{Binding CreateNoteCommand}"/>
                        <Button Content="Архивировать" Style="{StaticResource ModernButtonStyle}" Margin="0,0,10,0"
                                Command="{Binding ArchiveNotesCommand}"/>
                        <Button Content="Экспортировать" Style="{StaticResource ModernButtonStyle}"
                                Command="{Binding ExportNotesCommand}"/>
                    </StackPanel>
                </StackPanel>

                <!-- Список записей -->
                <ScrollViewer Grid.Column="1" Grid.Row="2" VerticalScrollBarVisibility="Auto">
                    <ListBox ItemsSource="{Binding Entries}" Style="{StaticResource ListBoxStyle}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource PaperHoverBrush}" CornerRadius="10" Padding="10"
                                        Margin="0,0,0,10" Effect="{StaticResource DropShadowEffect}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="16"
                                                   Style="{StaticResource BaseTextStyle}"/>
                                        <TextBlock Text="{Binding Content}" Style="{StaticResource BaseTextStyle}"
                                                   TextWrapping="Wrap" MaxHeight="50" Foreground="{StaticResource SecondaryBrush}"/>
                                        <TextBlock Text="{Binding Tags, Converter={StaticResource ListToStringConverter}}"
                                                   Style="{StaticResource BaseTextStyle}" FontSize="12"
                                                   Foreground="{StaticResource SecondaryBrush}"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </ScrollViewer>

                <!-- Кнопка возврата к чату -->
                <StackPanel Grid.Column="1" Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="{materialDesign:PackIcon Kind=Chat}" Style="{StaticResource IconButtonStyle}"
                            ToolTip="Вернуться к чату" Command="{Binding GoToChatCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Модальное окно для создания заметки -->
        <Border Background="{StaticResource ModalOverlayBrush}"
                Visibility="{Binding CreateNoteModalVisibility, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Style="{StaticResource BaseBorderStyle}" CornerRadius="10" Padding="20"
                    Width="400" Height="300" VerticalAlignment="Center" HorizontalAlignment="Center"
                    Effect="{StaticResource DropShadowEffect}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Новая заметка" FontSize="16" FontWeight="SemiBold" Style="{StaticResource BaseTextStyle}"
                               Margin="0,0,0,10"/>
                    <TextBox Grid.Row="1" Text="{Binding NewNoteContent, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource ModernTextBoxStyle}" AcceptsReturn="True" TextWrapping="Wrap"
                             materialDesign:HintAssist.Hint="Введите текст заметки..."/>
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Content="Отмена" Style="{StaticResource ModernButtonStyle}" Margin="0,0,10,0"
                                Command="{Binding CancelCreateNoteCommand}"/>
                        <Button Content="Сохранить" Style="{StaticResource ModernButtonStyle}"
                                Command="{Binding SaveNoteCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Border>

        <!-- Модальное окно для архивации -->
        <Border Background="{StaticResource ModalOverlayBrush}"
                Visibility="{Binding ArchiveNotesModalVisibility, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Style="{StaticResource BaseBorderStyle}" CornerRadius="10" Padding="20"
                    Width="300" Height="150" VerticalAlignment="Center" HorizontalAlignment="Center"
                    Effect="{StaticResource DropShadowEffect}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Архивировать все записи?" TextWrapping="Wrap"
                               Style="{StaticResource BaseTextStyle}" FontSize="14"/>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Content="Отмена" Style="{StaticResource ModernButtonStyle}" Margin="0,0,10,0"
                                Command="{Binding CancelArchiveNotesCommand}"/>
                        <Button Content="Архивировать" Style="{StaticResource ModernButtonStyle}"
                                Command="{Binding ConfirmArchiveNotesCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Border>
    </Grid>
</Page>